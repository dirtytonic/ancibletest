---
- hosts: [home]
  become: true
  become_method: sudo
  tasks:
   - name: make sqldump
     mysql_db: state=dump name=test target=/tmp/test.sql
   - name: copy dump to local
     fetch:
      src: /tmp/test.sql
      dest: /home/dirty_man/backup